class Car {
  constructor(name, year) {
    this.name = name;
    this.year = year;

    const plop = 1000;
    if (n >= plop) {
      this.plop(item);
    }
  }

  plop(item) {
    console.log(item)
  }
}